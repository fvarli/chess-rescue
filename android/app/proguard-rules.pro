# Project-specific R8/ProGuard rules for release builds.
#
# The Flutter Gradle plugin already contributes the keep rules required by the
# Flutter engine and embedding. This app uses only shared_preferences (platform
# channels, no reflection), so no additional keep rules are needed today.
#
# Add app-specific -keep rules here if a future plugin requires them.
