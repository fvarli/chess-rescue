# Archived — SD1 "The bishop on the line" (seal the diagonal)

## Original memo summary

**Motif.** `sealDiagonal`

**Proposed rescue.** `Rd4` — wR-d1 interposes on the a7-g1 diagonal at d4, blocking bB-a7's attack on wK-g1.

**Position.** wK g1; wP g2/h2 (no f2 — f2 vacant exposes the a7-g1 diagonal); bB a7 (the threat); wR d1 (rescuer); atmospheric pieces.

**Author's self-rating.** Weak. Concern: `Rd4` is geometrically identical to B1's `Rd4` rescue.

## Rejection record

- **Decision.** REJECTED.
- **Phase.** Phase 2 (memo-level, lead-directed: *çok agresif ol*).
- **Reviewer.** Lead.
- **Check that failed.** R0 — *instinct vs. memory* with B1 as the reference.
- **Rationale.** SD1 fails R0 completely. The proposed rescue `Rd4` is **geometrically identical** to B1 ("the martyr") — B1's rescue is also `Rd4` along the same diagonal interposing on bB's attack line. Same piece, same square, same diagonal, same mechanism.

  The only superficial difference is that B1 is tagged `forcedInterposition` (the rook is a martyr-sacrifice) whereas SD1 is tagged `sealDiagonal` (the rook is the seal). But at the *position-recognition* level, a player who has internalised B1's `Rd4` would IMMEDIATELY play `Rd4` on SD1's board — from pure position memory, with no motif-instinct exercise.

  Visual proximity to B1 is total. SD1 fails R0 in its sharpest form.
- **Future-sprint guidance.** Future `sealDiagonal` candidates must use:
  1. A different diagonal axis (not a7-g1 — try a2-g8, h7-b1, etc.)
  2. A different sealing piece (knight, bishop interposition — not rook)
  3. A different sealing square (not d4)

  The a7-g1 diagonal sealed by `Rd4` is permanently associated with B1; never re-author into the same shape.
